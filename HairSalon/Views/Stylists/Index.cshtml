@{
  Layout = "_Layout";
}

@using HairSalon.Models;

@if (@Model.Count == 0)
{
  <br />
  <br />
  <h2>No stylists available.</h2>
} 
    
@foreach (Stylist stylist in Model)
{
  <h2>@Html.ActionLink($"* {@stylist.Name}", "Details", new { id = stylist.StylistId })</h2>
}
